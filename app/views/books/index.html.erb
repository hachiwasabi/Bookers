<h1>投稿一覧</h1>

<% if @books.present? %>
  <% @books.each do |book| %>
    <p>タイトル</p>
    <span>
      <%= link_to book.title, "/books/#{book.id}" %>
    </span>
  <% end %>
<% else %>
  <p>error</p>
<% end %>